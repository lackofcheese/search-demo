package search.states;

/**
 * A simple interface representing a state within a state space. No methods are
 * needed, but any state should be uniquely identifiable, and states should be
 * immutable.
 * 
 * @author lackofcheese
 */
public interface State {
}